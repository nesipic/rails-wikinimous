<h2><%= @article.title %></h2>
<p><%= @article.content %></p>

<%= link_to "Update", edit_article_path(@article)  %>
<%= link_to "Delete #{@article.title}",
            article_path(@article),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
<%= link_to "Create", new_article_path  %>

<p> If you want to get back to the article list press <%= link_to "HERE", articles_path  %>